import React, { memo, Fragment } from 'react';

function Dashboard() {
    return (
        <Fragment>
            <div>
                Dashboard
            </div>
        </Fragment>
    )
}

export default memo(Dashboard)
